(function(){
 'use strict';
 const R=window.CWFleetManagementRules,copy=window.CWFleetManagementCopy,$=id=>document.getElementById(id),panel=$('fleetManager'),keys=['cristalwater_jwt','token','adminToken','cristalwater_user','user'],jobs={};
 let actor,invalid=false,suspended=false,key='',raw=null,pending=null,original=null,receipt=null,review=null,proposal=null,operation=null,target=null,data=null,loadedChoices=null,storageBlocked=false,state='loading',writeState='idle',language='pt',page=1,query={q:'',active:'true'};
 const t=k=>copy[language][k]||k,node=(tag,text)=>{const el=document.createElement(tag);el.textContent=text;return el;},busy=()=>!!jobs.write||!!jobs.review,blocked=()=>invalid||suspended||storageBlocked,unresolved=()=>!!pending&&!receipt;
 function identity(){const tokens=keys.slice(0,3).map(k=>localStorage.getItem(k)),token=tokens.find(Boolean),users=keys.slice(3).map(k=>localStorage.getItem(k)).filter(Boolean).map(JSON.parse);if(!token||tokens.some(v=>v&&v!==token))return null;const claims=JSON.parse(atob(token.split('.')[1].replace(/-/g,'+').replace(/_/g,'/'))),id=Number(claims.userId||claims.id);if(claims.role!=='ADMIN'||claims.principalType==='ENV_ADMIN'||!R.positive(id)||!Number.isFinite(claims.exp)||claims.exp*1000<=Date.now()||!users.length||users.some(u=>u.role!=='ADMIN'||Number(u.userId||u.id)!==id))return null;return {token,id,owner:'ADMIN:'+id,expires:claims.exp*1000,fingerprint:JSON.stringify([tokens,users.map(u=>[u.role,Number(u.userId||u.id)])])};}
 function abort(kind){const job=jobs[kind];if(job){job.controller.abort();clearTimeout(job.timer);delete jobs[kind];}}
 function clearFields(){$('fleetForm').reset();$('confirmFleet').checked=false;}
 function active(){let current;try{current=identity();}catch(_){}if(!invalid&&actor&&current?.fingerprint===actor.fingerprint&&actor.expires>Date.now())return true;if(!invalid||state!=='session'){invalid=true;for(const k of Object.keys(jobs))abort(k);clearFields();data=null;loadedChoices=null;review=null;proposal=null;original=null;pending=null;receipt=null;operation=null;target=null;state='session';writeState='session';render();window.dispatchEvent(new Event('cw:fleet-session-ended'));}return false;}
 function start(kind){abort(kind);const controller=new AbortController(),job={kind,controller,timer:setTimeout(()=>controller.abort(),30000)};jobs[kind]=job;return job;}
 const current=job=>active()&&!suspended&&jobs[job.kind]===job;function finish(job){clearTimeout(job.timer);if(jobs[job.kind]===job)delete jobs[job.kind];}
 async function request(path,job,config={}){const res=await fetch('/api/fleet-management'+path,{...config,headers:{Authorization:'Bearer '+actor.token,'Content-Type':'application/json'},cache:'no-store',redirect:'error',signal:job.controller.signal});if(!current(job))throw Error('stale');if([401,403].includes(res.status)){invalid=true;active();throw Error('session');}const trusted=res.headers.get('x-cw-fleet')==='fleet-management-v1'&&res.headers.get('x-cw-owner')===actor.owner&&res.headers.get('cache-control')==='private, no-store'&&(res.headers.get('content-type')||'').startsWith('application/json'),value=await res.json();if(!current(job)||!trusted)throw Error('unconfirmed');if(res.status!==200)throw Object.assign(Error('refused'),value?.ok===false&&value.version===1&&value.owner===actor.owner&&typeof value.code==='string'&&value.code.startsWith('FLEET_')?{status:res.status,code:value.code}:{});if(!R.base(value,actor.owner))throw Error('unconfirmed');return value;}
 const editFields=['plate','name','brand','model','year','currentKm','notes'],allFields=[...editFields,'status'];
 function fieldNames(){return ['CREATE','EDIT'].includes(operation)?[...editFields,...(operation==='CREATE'?['status']:[])]:['STATUS','RESTORE'].includes(operation)?['status']:[];}
 function build(){for(const name of allFields){const label=node('label',''),span=node('span',t(name)),input=node(name==='status'?'select':name==='notes'?'textarea':'input','');span.dataset.fleetCopy=name;label.id='fm-field-'+name;input.id='fm-'+name;input.name=name;if(input.tagName!=='SELECT')input.maxLength=R.limits[name]||30;if(input.tagName==='INPUT'){input.type='text';if(['year','currentKm'].includes(name))input.inputMode=name==='year'?'numeric':'decimal';}label.append(span,input);$('fleetFields').append(label);}}
 function statusOptions(){const input=$('fm-status'),selected=input.value;input.replaceChildren();for(const value of ['',...R.statuses]){const option=node('option',value?t(value):t('chooseStatus'));option.value=value;input.append(option);}input.value=selected;}
 function collect(){const fields={};for(const k of fieldNames()){const value=$('fm-'+k).value;if(['year','currentKm'].includes(k)){const v=value.trim();fields[k]=v===''?null:/^\d+(?:[.,]\d+)?$/.test(v)?Number(v.replace(',','.')):NaN;}else fields[k]=value;}return fields;}
 function choose(action,row=null){if(!active()||blocked()||busy()||pending||operation||!loadedChoices)return;operation=action;target=row;review=null;proposal=null;receipt=null;clearFields();statusOptions();if(row)for(const k of editFields)$('fm-'+k).value=row[k]??'';$('fm-status').value=action==='STATUS'&&R.statuses.includes(row?.status)?row.status:'';writeState='editing';render();$('fleetWritePanel').scrollIntoView({block:'start'});}
 function fact(dl,label,value){const div=node('div','');div.append(node('dt',t(label)),node('dd',value===null||value===undefined||value===''?t('missing'):typeof value==='boolean'?t(value?'yes':'no'):String(value)));dl.append(div);}
 function describe(dl,row){dl.replaceChildren();if(!row){dl.append(node('p',t('missing')));return;}for(const k of [...editFields,'status','active','archiveStatus'])fact(dl,k,k==='status'?t(row[k]):row[k]);}
 function date(v){return new Intl.DateTimeFormat({pt:'pt-PT',en:'en-GB',fr:'fr-FR',es:'es-ES',de:'de-DE'}[language],{dateStyle:'short',timeStyle:'short',timeZone:'Europe/Lisbon'}).format(new Date(v));}
 function render(){panel.dataset.state=invalid?'session':state;panel.dataset.writeState=writeState;document.documentElement.lang=language;document.title='Cristal Water — '+t('title');for(const el of document.querySelectorAll('[data-fleet-copy]'))el.textContent=t(el.dataset.fleetCopy);$('fleetLanguage').value=language;for(const a of document.querySelectorAll('[data-destination]'))a.href=a.dataset.destination+'?lang='+language;statusOptions();$('fleetWritePanel').hidden=!operation&&!pending&&!storageBlocked;$('writeStatus').textContent=t(writeState);$('writeReference').textContent=pending?t('reference')+': '+pending.requestId+(receipt?.vehicle?' · #'+receipt.vehicle.id:''):'';$('operationTitle').textContent=operation?t(operation):'';$('targetIdentity').textContent=target?'#'+target.id+' — '+target.plate:'';
 $('fleetForm').hidden=!operation||blocked()||!!review||!!pending;for(const k of allFields){$('fm-field-'+k).hidden=!fieldNames().includes(k);$('fm-'+k).disabled=blocked()||busy()||!!review||!!pending;}for(const b of $('fleetForm').querySelectorAll('button'))b.disabled=blocked()||busy()||!!pending;
 $('fleetReview').hidden=!review||blocked()||!!pending;describe($('reviewBefore'),review?.before);describe($('reviewAfter'),review?.proposed);$('reviewTarget').textContent=review?t(review.operation)+(review.vehicleId?' · #'+review.vehicleId:''):'';$('reviewUsage').replaceChildren();if(review)for(const k of Object.keys(review.usage))fact($('reviewUsage'),k,review.usage[k]);$('reviewExpiry').textContent=review?t('expires')+' '+date(review.expiresAt):'';$('saveFleet').disabled=blocked()||busy()||!review||!$('confirmFleet').checked;$('backToFields').disabled=busy();$('confirmFleet').disabled=busy();
 for(const id of ['checkWrite','retryWrite','cancelWrite'])$(id).hidden=!unresolved()||blocked()||id==='retryWrite'&&!original;$('cancelHint').hidden=!unresolved()||blocked();$('nextWrite').hidden=!receipt||blocked();for(const id of ['checkWrite','retryWrite','cancelWrite','nextWrite'])$(id).disabled=busy();$('discardPreparation').hidden=!operation||!!pending||blocked();$('discardPreparation').disabled=busy();$('newFleet').disabled=blocked()||busy()||!!pending||!!operation||!loadedChoices;$('fleetStatus').textContent=t(invalid?'session':state);$('fleetCount').textContent=data?t('total')+': '+data.total:'';$('fleetPage').textContent=data?t('page')+' '+data.page+' / '+Math.max(1,Math.ceil(data.total/25)):'';$('fleetPrevious').disabled=blocked()||!data||data.page<=1;$('fleetNext').disabled=blocked()||!data||data.page*25>=data.total;$('refreshFleets').disabled=invalid||suspended;for(const el of $('fleetFilters').elements)el.disabled=invalid||suspended;
 const list=$('fleetList');list.replaceChildren();if(data){if(!data.vehicles.length)list.append(node('p',t('empty')));for(const row of data.vehicles){const card=node('article',''),dl=node('dl',''),actions=node('div','');card.className='hub-card';card.dataset.vehicleId=row.id;card.dataset.riskTargetType='Vehicle';card.dataset.riskTargetId=row.id;card.dataset.riskVehicleId=row.id;card.append(node('h3',row.plate),node('p','#'+row.id+' · '+t(row.active?'active':'inactive')));for(const k of ['name','brand','model','year','currentKm','status','archiveStatus'])fact(dl,k,k==='status'?t(row[k]):row[k]);actions.className='hub-actions';for(const action of ['EDIT',...(row.active&&!row.deletedAt?['STATUS','ARCHIVE']:['RESTORE'])]){const b=node('button',t(action));b.type='button';b.dataset.fleetAction=action;b.disabled=blocked()||busy()||!!pending||!!operation||!loadedChoices;b.addEventListener('click',()=>choose(action,row));actions.append(b);}card.append(dl,actions);list.append(card);}}
 }
 async function load(){if(!active()||suspended)return;data=null;loadedChoices=null;state='loading';const job=start('list'),requested=page,filter={...query};render();try{const rows=await request('?'+new URLSearchParams({...filter,page:requested}),job);if(!R.listPacket(rows,actor.owner,requested))throw Error();data=rows;loadedChoices={};state='ready';}catch(_){if(current(job)){data=null;loadedChoices=null;state='error';}}finally{finish(job);if(active()&&!suspended)render();}}
 function restore(){try{raw=sessionStorage.getItem(key);const record=R.pending(raw,actor.owner);if(record===false){storageBlocked=true;writeState='corrupt';return;}pending=record;original=null;receipt=null;writeState=pending?'unknown':'idle';}catch(_){storageBlocked=true;writeState='storage';}}
 function remember(value){try{if(sessionStorage.getItem(key)!==raw){storageBlocked=true;writeState='corrupt';return false;}const next=value===null?null:JSON.stringify(value);if(next===null)sessionStorage.removeItem(key);else sessionStorage.setItem(key,next);if(sessionStorage.getItem(key)!==next)throw Error();raw=next;return true;}catch(_){writeState='storage';return false;}}
 async function prepare(){if(!active()||blocked()||busy()||pending||!operation)return;const fields=collect();if(!R.fields(operation,fields)){writeState='invalid';render();return;}proposal={operation,vehicleId:target?.id||null,fields};review=null;$('confirmFleet').checked=false;writeState='reviewing';const job=start('review');render();try{const value=await request('/review',job,{method:'POST',body:JSON.stringify(proposal)});if(!R.reviewPacket(value,actor.owner,operation,target?.id||null))throw Error();review=value;writeState='reviewed';}catch(_){if(current(job)){review=null;proposal=null;writeState='reviewError';}}finally{finish(job);if(active()&&!suspended)render();}}
 function accept(value){if(!pending||!R.resultPacket(value,actor.owner,pending))throw Error();if(value.status==='UNCONFIRMED'){writeState='unconfirmed';return;}receipt=value.result;writeState=value.status==='CANCELLED'?'cancelled':'saved';review=null;proposal=null;original=null;clearFields();if(value.status==='CONFIRMED')window.dispatchEvent(new Event('cw:fleet-updated'));}
 async function send(retry=false){if(!active()||blocked()||busy()||(retry?!pending||!original:!!pending||!review||!$('confirmFleet').checked))return;if(!retry){if(Date.parse(review.expiresAt)<=Date.now()){review=null;proposal=null;writeState='expired';render();return;}const candidate={version:1,owner:actor.owner,operation,vehicleId:target?.id||null,requestId:review.requestId};if(!remember(candidate)){render();return;}pending=candidate;original=JSON.stringify({...proposal,requestId:review.requestId,reviewToken:review.reviewToken});}writeState='saving';const job=start('write');render();try{accept(await request('/commit',job,{method:'POST',body:original}));}catch(e){if(current(job))writeState=e.status===409?'changed':'unknown';}finally{finish(job);if(active()&&!suspended){render();if(receipt)load();}}}
 async function check(cancel=false){if(!active()||blocked()||busy()||!unresolved())return;const job=start('write');writeState='checking';render();try{accept(await request('/'+(cancel?'cancel':'result')+'/'+pending.requestId,job,cancel?{method:'POST',body:JSON.stringify({operation:pending.operation,vehicleId:pending.vehicleId})}:{}));}catch(_){if(current(job))writeState='unknown';}finally{finish(job);if(active()&&!suspended){render();if(receipt)load();}}}
 build();const lang=new URL(location.href).searchParams.get('lang');if(Object.hasOwn(copy,lang))language=lang;try{actor=identity();if(!actor)throw Error();key='cw:fleet-management:v1:'+actor.owner;}catch(_){invalid=true;}
 $('discardPreparation').addEventListener('click',()=>{if(active()&&!blocked()&&!busy()&&!pending){clearFields();operation=null;target=null;review=null;proposal=null;writeState='idle';render();}});$('newFleet').addEventListener('click',()=>choose('CREATE'));$('fleetForm').addEventListener('submit',e=>{e.preventDefault();prepare();});$('confirmFleet').addEventListener('change',render);$('saveFleet').addEventListener('click',()=>send());$('backToFields').addEventListener('click',()=>{if(active()&&!busy()&&!pending){review=null;proposal=null;$('confirmFleet').checked=false;writeState='editing';render();}});$('checkWrite').addEventListener('click',()=>check());$('retryWrite').addEventListener('click',()=>send(true));$('cancelWrite').addEventListener('click',()=>check(true));$('nextWrite').addEventListener('click',()=>{if(active()&&!blocked()&&!busy()&&receipt&&remember(null)){pending=null;receipt=null;original=null;operation=null;target=null;review=null;proposal=null;writeState='idle';}render();});
 $('fleetFilters').addEventListener('submit',e=>{e.preventDefault();query={q:$('fleetSearch').value.trim(),active:$('fleetActive').value};page=1;load();});$('clearFleetSearch').addEventListener('click',()=>{$('fleetSearch').value='';$('fleetActive').value='true';query={q:'',active:'true'};page=1;load();});$('refreshFleets').addEventListener('click',load);$('fleetPrevious').addEventListener('click',()=>{page=Math.max(1,page-1);load();});$('fleetNext').addEventListener('click',()=>{page++;load();});$('fleetLanguage').addEventListener('change',()=>{language=$('fleetLanguage').value;const url=new URL(location.href);url.searchParams.set('lang',language);history.replaceState(null,'',url);render();});
 window.addEventListener('storage',active);window.addEventListener('focus',active);document.addEventListener('visibilitychange',()=>{if(!document.hidden)active();});window.addEventListener('pagehide',()=>{suspended=true;for(const k of Object.keys(jobs))abort(k);clearFields();review=null;proposal=null;original=null;receipt=null;data=null;loadedChoices=null;operation=null;target=null;state='loading';writeState=pending?'unknown':'idle';render();});window.addEventListener('pageshow',()=>{suspended=false;if(active()){restore();render();load();}});setInterval(()=>{if(active()&&review&&!pending&&Date.parse(review.expiresAt)<=Date.now()){review=null;proposal=null;writeState='expired';render();}},1000);
 window.CWFleetPageSession={isCurrent:()=>active()&&!suspended};
 for(const id of ['writeStatus','writeReference','reviewBefore','reviewAfter','fleetStatus','fleetList'])$(id).dataset.cwStateManaged='manual';render();active();
}());
